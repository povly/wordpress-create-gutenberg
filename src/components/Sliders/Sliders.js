import {useState, useEffect} from "@wordpress/element";

const Sliders = (props) => {
    return (
        <div className={ props.className }>
            Sliders
        </div>
    );
};

export default Sliders;